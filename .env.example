# HubSpot (server-side only, never exposed to the client)
HUBSPOT_API_KEY=

# Fallback lead queue — Vercel KV (or any Upstash-compatible REST KV store)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: Plausible analytics domain, if self-hosting the script
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=qavyon.com
